{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=prod node src/server.mjs",
    "dev": "NODE_ENV=dev nodemon src/server.mjs",
    "db:create": "mysql -u root -p < ./scripts/create.sql",
    "db:update": "mysql -u root -p < ./scripts/update.sql",
    "db:seed": "NODE_ENV=dev node ./scripts/seed.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv-flow": "^4.1.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mysql2": "^3.15.2",
    "winston": "^3.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
